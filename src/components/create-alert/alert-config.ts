/**
 * alert-config.ts — backward-compatible re-export.
 * Config is split into ./config/* for maintainability.
 * Add categories in ./config/index.ts, not here.
 */
export { ALERT_CONFIG } from "./config";
